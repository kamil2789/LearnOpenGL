#pragma once
#include <chrono>

namespace ProgramExamples
{
    bool twoGreenTriangles(GLFWwindow* window, std::chrono::seconds duration);
    bool rainbowTriangle(GLFWwindow* window, std::chrono::seconds duration);
}